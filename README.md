# Plakar Backblaze Plugin

```sh
$ make uninstall package install
$ plakar store add b2 b2://<BUCKET_NAME> key_id=<KEY_ID> app_key=<APP_KEY>
$ plakar at @b2 create
$ plakar at @b2 backup ~/Downloads
$ plakar at @b2 ls
$ plakar at @b2 restore -to pop
```